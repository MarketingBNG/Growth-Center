# Shell scripts run on Linux (the GCP VM) — a Windows CRLF in the shebang
# line makes bash fail with "bad interpreter: No such file or directory".
# Force LF regardless of the checking-out machine's core.autocrlf setting.
*.sh text eol=lf
