node_modules
.next
out
.env
.env.local
.env*.local
lib/generated
*.tsbuildinfo
next-env.d.ts
.DS_Store
screenshots
playwright-report
test-results

# Generated by Next 16 on dev start
/AGENTS.md
/CLAUDE.md

# Meeting notes and transcripts. Kept locally: git history is permanent, and these are
# recordings of named people.
/New

# Playwright scratch: throwaway specs and screenshots taken while verifying a page.
# One of these was committed by accident, which is why it is written down.
e2e/tmp/
test-results/

# Personal-GCP-project deploy test notes and rendered compose files — reference
# a personal test project's IDs/paths, not meant to be shared in the repo.
deploy/MANUAL-DEPLOY-NOTES.md
deploy/docker-compose.rendered.yml
