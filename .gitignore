node_modules
.next
out
.env
.env.local
.env*.local
lib/generated
*.tsbuildinfo
next-env.d.ts
.DS_Store
screenshots
playwright-report
test-results

# Generated by Next 16 on dev start
/AGENTS.md
/CLAUDE.md

# Meeting notes and transcripts. Kept locally: git history is permanent, and these are
# recordings of named people.
/New

# Playwright scratch: throwaway specs and screenshots taken while verifying a page.
# One of these was committed by accident, which is why it is written down.
e2e/tmp/
test-results/

# Implementation pack for the Organic Growth Automation Engine: prompts, target schema
# and checklists handed over as a working reference, not source. Kept local so the repo
# keeps only what it builds, not the brief it was built from.
/Growth_Center_Claude_Code_Pack
